spring:
  datasource:
    url: java
    username: ${ILYA_ZUEV_USER}
    password: ${ILYA_ZUEV_PASS}
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
---

server:
  port: 8081

